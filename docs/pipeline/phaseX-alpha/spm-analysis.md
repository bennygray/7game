# Phase X-α SPM 分析 — 交互与展现层梳理

> **Phase**：X-α "掌门视界" · 架构拆分 + 信息分区
> **创建日期**：2026-03-31
> **SPM 状态**：进行中

---

## §1 USER 原始需求

> "基本的框架其实已经搭建的差不多了，我想对我的交互和展现进行一次完整的梳理"
> "信息没有分级，也没有分页，杂乱而毫无意义。虽然号称世界已经有了血肉，掌门有了参与，但是完全看不出来"

### USER 决策记录（2026-03-31 01:40）

| # | 问题 | USER 决策 |
|---|------|----------|
| Q1 | 改造范围 | 先做 X-α（架构拆分 + 信息分区） |
| Q2 | GUI vs MUD | 允许引入 GUI 元素；**不希望引入重 DOM 框架**（后续会迁移 Electron，届时再整体重构前端） |
| Q3 | 日志过滤策略 | B）折叠到系统消息区 — "这是目前最让人迷茫的东西" |
| Q4 | 输入框位置 | 移到底部 |
| Q5 | 流程规范 | 要走完整 Trinity Pipeline（SPM→SGA→SGE） |

---

## §2 现有架构盘点

### 文件清单

| 文件 | 行数 | 字节 | 职责 |
|------|:----:|:----:|------|
| `src/main.ts` | 606 | 22069 | **巨石** — UI 初始化 + DOM + 命令解析 + 引擎回调 + AI 连接 + 存档 + 全局状态 |
| `src/ui/mud-formatter.ts` | 448 | 18020 | 纯函数格式化（look/inspect/sect/judge/severity/statusBar/ambient） |
| `index.html` | 14 | 414 | 空壳（`<div id="app">` + 脚本引入） |

### UI 组件（全部内联在 main.ts L78-147）

| 组件 | DOM ID | 实现 |
|------|--------|------|
| 状态栏 | `#status-bar` | sticky div，纯文本，L91-104 |
| ASCII 标题 | `#mud-screen` | `<pre>` 框线，L105-114 |
| 命令输入 | `#cmd-input` | `<input>` + Enter，L116-146 |
| 日志区 | `#mud-log` | div，innerHTML 拼接，L127-135 |

### 命令系统（10 个，main.ts L226-411）

| 命令 | 快捷 | 功能 |
|------|------|------|
| `look [弟子]` | `l` | 宗门总览 / 弟子档案 |
| `inspect <弟子>` | `i` | 灵魂档案 |
| `sect` | — | 宗门道风 |
| `judge [N]` | — | 裁决窗口 |
| `status` | — | 宗主状态 |
| `bt` / `breakthrough` | — | 尝试突破 |
| `clear` | — | 清空日志 |
| `reset` | — | 清除存档 |
| `ai` | — | 连接 AI 后端 |
| `help` | — | 帮助 |

### 回调管线（8 条回调 + 1 定时器，main.ts L420-598）

| 回调 | 来源 | 输出内容 |
|------|------|----------|
| `onTick` | 每秒 | 更新状态栏 + 每 5s 修炼日志 |
| `onBreakthrough` | 突破引擎 | STORM/SPLASH 级日志 |
| `onFarmTickLog` | 灵田 handler | RIPPLE 级日志 |
| `onSystemLog` | 系统 handler | RIPPLE 级日志 |
| `onDialogue` | 对话协调器 | 彩色对话组 |
| `onMudLog` | 统一管线 | severity 路由日志 |
| `onDiscipleBehaviorChange` | 弟子行为树 | 行为变更 + AI 台词 |
| `onRulingCreated/Resolved` | 裁决系统 | 裁决窗口/结果 |
| `scheduleAmbientBreath` | 25-45s 定时 | BREATH 级呼吸文案 |

### 样式现状

- **零 CSS 文件** — 全部通过 JS 内联 `style="..."` 硬编码
- 20+ 颜色值散落在 main.ts 和 mud-formatter.ts 两个文件
- 颜色方案：`#1a1a2e`(bg), `#0d0d1a`(dark), `#c8b88b`(text), `#8a8a6a`(dim), `#7a8aba`(accent), `#ffd700`(gold), `#ff4444`(red), `#50fa7b`(green), `#8bc8c8`(cyan) 等

---

## §3 问题诊断

### P0 架构层

| # | 问题 | 影响 | 证据 |
|---|------|------|------|
| X-01 | main.ts 606 行巨石（6 种职责） | 可维护性极差 | UI/命令/回调/AI/存档/全局状态混合 |
| X-02 | 零 CSS 文件，样式内联 JS | 无法主题化或复用 | `style="..."` 硬编码 |
| X-03 | 旧回调与统一管线并存 | TD-015 已标记，信息可能重复 | 8 条回调 |

### P1 体验层（USER 最大痛点）

| # | 问题 | 影响 |
|---|------|------|
| X-04 | **日志信噪比极低** — 修炼灵气/soul-engine 内部评估/弟子对话/世界事件/环境呼吸全混一起 | 重要事件被淹没，`[soul-engine]` 对玩家无意义 |
| X-05 | **信息密度不足** — look/inspect/sect 输出插入日志流后秒被冲走 | 查了弟子档案 5 秒后就看不见了 |
| X-06 | 命令系统简陋（无历史、无补全） | 操作效率低 |
| X-07 | 零视觉反馈（突破/STORM 只换颜色） | 关键事件无存在感 |
| X-08 | 单列垂直堆叠布局 | 屏幕利用率低 |

### P2 细节

| # | 问题 |
|---|------|
| X-09 | 日志区 `max-height: 400px` 在大屏浪费/小屏不够 |
| X-10 | 状态栏信息过于紧凑 |
| X-11 | 颜色方案缺乏统一设计 |
| X-12 | 输入框在标题和日志之间，MUD 惯例应在底部 |

---

## §4 约束与边界

### 硬约束（USER 明确）

| # | 约束 | 来源 |
|---|------|------|
| HC-1 | 不引入重 DOM 框架 | USER Q2 决策（Electron 迁移预期） |
| HC-2 | 完全不动 Engine/AI/Data 层 | 改造范围限定展现层 |
| HC-3 | 零 GameState 变更，零存档迁移 | 纯 UI 重构 |
| HC-4 | 走完整 Trinity Pipeline | USER Q5 决策 |

### 软约束

| # | 约束 | 理由 |
|---|------|------|
| SC-1 | 保持 MUD 文字基调 | 游戏定位 |
| SC-2 | main.ts 目标 < 150 行 | 可维护性 |
| SC-3 | 允许 GUI 元素（面板/进度条） | USER Q2 决策 |

# Phase X-γ User Stories — 面板系统 + 内存泄漏修复

> **Phase**：X-γ "掌门视界·面板系统 + 关键性能修复"
> **PRD**：[phaseX-gamma-PRD.md](../../features/phaseX-gamma-PRD.md)
> **创建日期**：2026-03-31

---

## Story 0：内存泄漏修复（P0 BUG-Xγ-01）

> **作为**玩家，**我需要**游戏不再导致电脑死机，**以便**我能正常挂机玩耍。

| AC# | Given | When | Then | Data Anchor |
|:---:|-------|------|------|-------------|
| 1 | 引擎运行中 | `addMainLog()` 被调用 | 仅创建 1 个 `<span>` 节点并 `appendChild`，不使用 `container.innerHTML =` 全量替换 | PRD §2.8 M-FIX |
| 2 | 主日志区已有 200 行 | `addMainLog()` 被调用 | 最老的 1 行被 `removeChild`，总行数保持 ≤200 | PRD §2.8 MAIN_LOG_MAX=200 |
| 3 | 系统消息条已有 5 行 | `addSystemLog()` 被调用 | 最老的 1 行被 `removeChild`，总行数保持 ≤5 | PRD §2.8 SYSTEM_LOG_MAX=5 |
| 4 | `log-manager.ts` 修复后 | 检查源码 | `mainLines[]` 和 `systemLines[]` JS 数组已移除，DOM 即唯一状态源 | PRD §2.8 |
| 5 | 引擎运行 10 分钟 | 观察内存 | 内存稳态（不线性增长） | — |

- **依赖**：无（最高优先级，先于所有其他 Story）
- **复杂度**：S

---

## Story 1：面板容器基础设施

> **作为**掌门，**我需要**一个浮层面板容器，**以便**结构化信息能停留在视野中而不被日志冲走。

| AC# | Given | When | Then | Data Anchor |
|:---:|-------|------|------|-------------|
| 1 | 游戏已启动 | DOM 加载完成 | `.mud-panel-overlay` 元素存在于 `.mud-container` 内，且 `display: none` | PRD §2.3 P-DOM |
| 2 | 面板关闭状态 | 调用 `openPanel('标题', '<div>内容</div>')` | Overlay 变为 `display: flex`，面板标题显示"标题"，body 包含传入 HTML | PRD §2.7 P-API |
| 3 | 面板打开状态 | 调用 `closePanel()` | Overlay 变为 `display: none` | PRD §2.2 L-01 |
| 4 | 面板 A 打开状态 | 调用 `openPanel('新标题', '新内容')` | 面板 A 被替换为新内容（I3 不变量：同时最多一个面板） | PRD §2.2 L-01 |
| 5 | 面板打开状态 | 按下 ESC 键 | 面板关闭 | PRD §2.2 L-02 |
| 6 | 面板打开状态 | 点击 Overlay 背景（面板外区域） | 面板关闭 | PRD §2.2 L-03 |
| 7 | 面板打开状态 | 点击面板内部区域 | 面板保持打开（事件不冒泡到 Overlay） | — |

- **依赖**：无（基础设施）
- **复杂度**：M

---

## Story 2：look 命令面板化

> **作为**掌门，**我需要** `look` 和 `look <弟子>` 的输出显示在面板中，**以便**我能持续参考宗门总览或弟子档案。

| AC# | Given | When | Then | Data Anchor |
|:---:|-------|------|------|-------------|
| 1 | 无面板打开 | 输入 `look` | 打开 P1 面板，标题"宗门总览"，内容为 `formatLookOverview()` 的输出 | PRD §2.1 P1 + §2.5 |
| 2 | 无面板打开 | 输入 `look 张清风` | 打开 P2 面板，标题"弟子档案 · 张清风"，内容为 `formatDiscipleProfile()` | PRD §2.1 P2 + §2.5 |
| 3 | P1 面板打开 | 输入 `look 张清风` | P1 关闭 → P2 打开（L-01 + L-04） | PRD §2.2 L-04 |
| 4 | 面板打开 | 日志流收到新消息 | 面板保持不动，日志在背后正常更新 | PRD §2.2 L-05 / I1 |
| 5 | — | 输入 `look` + 输入 `l` | 行为一致（别名兼容） | — |

- **依赖**：Story 1
- **复杂度**：S

---

## Story 3：inspect 命令面板化

> **作为**掌门，**我需要** `inspect <弟子>` 的灵魂档案显示在面板中，**以便**我能仔细阅读弟子的五维性格、道德、特性和关系。

| AC# | Given | When | Then | Data Anchor |
|:---:|-------|------|------|-------------|
| 1 | 无面板打开 | 输入 `inspect 张清风` | 打开 P3 面板，标题"灵魂档案 · 张清风"，内容为 `formatDiscipleInspect()` | PRD §2.1 P3 + §2.5 |
| 2 | 无面板打开 | 输入 `i 张` | 前缀匹配 + 别名生效，打开 P3 面板 | — |
| 3 | — | 输入 `inspect` 无参数 | 系统提示用法（行为不变 — 依然写入日志流） | — |
| 4 | P3 面板打开 | 面板内容超过 70vh | 面板内部可滚动（`overflow-y: auto`） | PRD §2.4 |

- **依赖**：Story 1
- **复杂度**：S

---

## Story 4：sect 命令面板化

> **作为**掌门，**我需要** `sect` 的宗门道风输出显示在面板中，**以便**我能持续参考双轴刻度尺。

| AC# | Given | When | Then | Data Anchor |
|:---:|-------|------|------|-------------|
| 1 | 无面板打开 | 输入 `sect` | 打开 P4 面板，标题"宗门道风"，内容为 `formatSectProfile()` | PRD §2.1 P4 + §2.5 |

- **依赖**：Story 1
- **复杂度**：S

---

## Story 5：judge 命令面板化 + 自动推送

> **作为**掌门，**我需要**裁决窗口显示在面板中，且 STORM 事件触发时自动弹出，**以便**我不会错过裁决窗口并能从容做出决策。

| AC# | Given | When | Then | Data Anchor |
|:---:|-------|------|------|-------------|
| 1 | 有活跃裁决 | 输入 `judge`（无参数） | 打开 P5 面板，标题"⚡ 风暴裁决"，内容为 `formatRulingWindow()` | PRD §2.1 P5 + §2.5 |
| 2 | P5 面板打开 | 输入 `judge 1` | 裁决执行 + 面板关闭 + 裁决结果写入日志流 | PRD §2.2 L-08 + §2.6 |
| 3 | STORM 事件触发 | `onRulingCreated` 回调 | 自动打开 P5 面板（若已有其他面板 → 先关闭） | PRD §2.2 L-06 |
| 4 | P5 面板打开（裁决活跃） | 裁决超时被 engine 解决 | 面板自动关闭 + 超时结果写入日志流 | PRD §2.2 L-07 |
| 5 | 无活跃裁决 | 输入 `judge` | 系统提示"无需裁决"（行为不变 — 写入日志流） | — |

- **依赖**：Story 1
- **复杂度**：M

---

## Story 6：可点击弟子名

> **作为**掌门，**我需要**点击日志或面板中的弟子姓名就能弹出灵魂档案面板，**以便**我能快速查看弟子详情而不必手动输入命令。

| AC# | Given | When | Then | Data Anchor |
|:---:|-------|------|------|-------------|
| 1 | 日志流中有弟子行为日志 `[张清风] 开始打坐修炼` | 鼠标悬停"张清风" | 出现下划线 + 指针变为 pointer | PRD §2.9 CK-01 |
| 2 | 日志流中有弟子行为日志 | 点击"张清风" | 打开 P3 面板（灵魂档案·张清风），等价于 `inspect 张清风` | PRD §2.9 CK-02 |
| 3 | P2 面板（look 总览）中列出弟子名 | 点击弟子名 | P2 关闭 → P3 面板打开（L-01 + CK-03） | PRD §2.9 CK-03 |
| 4 | 日志中有弟子间对话 | 点击对话中的说话者名字 | 打开对应弟子的 P3 面板 | PRD §2.9 |
| 5 | — | 检查源码 | 弟子名使用 `<span class="mud-disciple-link" data-disciple-id="...">` 渲染 | PRD §2.9 CK-04 |

- **依赖**：Story 1（面板基础设施）
- **复杂度**：M

---

## Story 7：全局回归

> **作为**开发者，**我需要**确认 Phase X-γ 的改动不破坏现有系统，**以便**安全发布。

| AC# | Given | When | Then | Data Anchor |
|:---:|-------|------|------|-------------|
| 1 | 所有代码改动完成 | 运行 `npx tsc --noEmit` | 零编译错误 | — |
| 2 | 所有代码改动完成 | 运行 `npx tsx scripts/regression-all.ts` | 64/64 通过 | — |
| 3 | — | 计算 main.ts 行数 | ≤ 150 行 | HC-4 |
| 4 | — | 检查 GameState 类型 | 零变更 | HC-3 |
| 5 | 引擎运行 10 分钟 | 检查浏览器内存 | 无线性增长（BUG-Xγ-01 回归） | PRD §2.8 |

- **依赖**：Story 0~6
- **复杂度**：S
